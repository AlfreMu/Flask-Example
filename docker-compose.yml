version: "3.9"

services:
  web:
    
    image: flask-example:local
    ports:
      - "5000:5000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5000/"]
      interval: 30s
      timeout: 3s
      retries: 3
    restart: unless-stopped
